/*Estudando os conceitos de For para C#
 * For (uma regra para repetir){executar ate a regra for valida}
 * 
 * for(e1;e2;e3)
 * e1 => é o que marca o inicia do loop. Inicializador (ou contador)
 * e2 => é o que define a regra. Enquanto essa regra for valida, o loop continua ativo.
 * e3 => é o incremento (ou decremento) da expressão
 */